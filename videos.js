function setup() {
  // Sketch 1
  let canvas1 = createCanvas(300, 300);
  canvas1.parent('sketch1');
  background(255, 0, 0); // Red background for Sketch 1
  
  // Sketch 2
  let canvas2 = createCanvas(300, 300);
  canvas2.parent('sketch2');
  background(0, 255, 0); // Green background for Sketch 2
}
